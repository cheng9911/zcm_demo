// THIS IS AN AUTOMATICALLY GENERATED FILE.
// DO NOT MODIFY BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#ifndef ZCM_EMBEDDED
#include <stdio.h>
#endif
#include "types/foo/foo_t1.h"

static int __foo_t1_hash_computed = 0;
static uint64_t __foo_t1_hash;

uint64_t __foo_t1_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __foo_t1_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__foo_t1_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x1234567802743100LL
         + __foo_t2_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __foo_t1_get_hash(void)
{
    if (!__foo_t1_hash_computed) {
        __foo_t1_hash = (int64_t)__foo_t1_hash_recursive(NULL);
        __foo_t1_hash_computed = 1;
    }

    return __foo_t1_hash;
}

int __foo_t1_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const foo_t1* p, uint32_t elements)
{
    uint32_t pos_byte = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        /* x */
        thislen = __foo_t2_encode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].x), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

    }
    return pos_byte;
}

int foo_t1_encode(void* buf, uint32_t offset, uint32_t maxlen, const foo_t1* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __foo_t1_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __foo_t1_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __foo_t1_encoded_array_size(const foo_t1* p, uint32_t elements)
{
    uint32_t size = 0, element;
    for (element = 0; element < elements; ++element) {

        size += __foo_t2_encoded_array_size(&(p[element].x), 1); // x

    }
    return size;
}

uint32_t foo_t1_encoded_size(const foo_t1* p)
{
    return 8 + __foo_t1_encoded_array_size(p, 1);
}

uint32_t foo_t1_struct_size(void)
{
    return sizeof(foo_t1);
}

uint32_t foo_t1_num_fields(void)
{
    return 1;
}

int foo_t1_get_field(const foo_t1* p, uint32_t i, zcm_field_t* f)
{
    if (i >= foo_t1_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            /* t2 */
            f->name = "x";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "foo.t2";
            f->num_dim = 0;
            f->data = (void*) &p->x;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* foo_t1_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) foo_t1_encode;
        typeinfo.decode         = (zcm_decode_t) foo_t1_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) foo_t1_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) foo_t1_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  foo_t1_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) foo_t1_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) foo_t1_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __foo_t1_get_hash;
    }
    
    return &typeinfo;
}

int __foo_t1_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, foo_t1* p, uint32_t elements)
{
    uint32_t pos_byte = 0, element;
    int thislen;

    for (element = 0; element < elements; ++element) {

        /* x */
        thislen = __foo_t2_decode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].x), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

    }
    return pos_byte;
}

int __foo_t1_decode_array_cleanup(foo_t1* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __foo_t2_decode_array_cleanup(&(p[element].x), 1);

    }
    return 0;
}

int foo_t1_decode(const void* buf, uint32_t offset, uint32_t maxlen, foo_t1* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __foo_t1_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __foo_t1_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int foo_t1_decode_cleanup(foo_t1* p)
{
    return __foo_t1_decode_array_cleanup(p, 1);
}

uint32_t __foo_t1_clone_array(const foo_t1* p, foo_t1* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __foo_t2_clone_array(&(p[element].x), &(q[element].x), 1);

    }
    return n;
}

foo_t1* foo_t1_copy(const foo_t1* p)
{
    foo_t1* q = (foo_t1*) malloc(sizeof(foo_t1));
    __foo_t1_clone_array(p, q, 1);
    return q;
}

void foo_t1_destroy(foo_t1* p)
{
    __foo_t1_decode_array_cleanup(p, 1);
    free(p);
}

int foo_t1_publish(zcm_t* zcm, const char* channel, const foo_t1* p)
{
      uint32_t max_data_size = foo_t1_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = foo_t1_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _foo_t1_subscription_t {
    foo_t1_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void foo_t1_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    foo_t1 p;
    memset(&p, 0, sizeof(foo_t1));
    status = foo_t1_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        #ifndef ZCM_EMBEDDED
        fprintf (stderr, "error %d decoding foo_t1!!!\n", status);
        #endif
        return;
    }

    foo_t1_subscription_t* h = (foo_t1_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    foo_t1_decode_cleanup (&p);
}

foo_t1_subscription_t* foo_t1_subscribe (zcm_t* zcm,
                    const char* channel,
                    foo_t1_handler_t f, void* userdata)
{
    foo_t1_subscription_t* n = (foo_t1_subscription_t*)
                       malloc(sizeof(foo_t1_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              foo_t1_handler_stub, n);
    if (n->z_sub == NULL) {
        #ifndef ZCM_EMBEDDED
        fprintf (stderr,"couldn't reg foo_t1 ZCM handler!\n");
        #endif
        free (n);
        return NULL;
    }
    return n;
}

int foo_t1_unsubscribe(zcm_t* zcm, foo_t1_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        #ifndef ZCM_EMBEDDED
        fprintf(stderr,
           "couldn't unsubscribe foo_t1_handler %p!\n", hid);
        #endif
        return -1;
    }
    free (hid);
    return 0;
}

