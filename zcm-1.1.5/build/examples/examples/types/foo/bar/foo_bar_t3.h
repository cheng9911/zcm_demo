// THIS IS AN AUTOMATICALLY GENERATED FILE.
// DO NOT MODIFY BY HAND!!
//
// Generated by zcm-gen

#include <stdint.h>
#include <stdlib.h>
#include <zcm/zcm_coretypes.h>
#include <zcm/zcm.h>

#ifndef _foo_bar_t3_h
#define _foo_bar_t3_h

#ifdef __cplusplus
extern "C" {
#endif

#include "types/unpackaged_t.h"
#define FOO_BAR_T3_IS_LITTLE_ENDIAN 0
typedef struct _foo_bar_t3 foo_bar_t3;
struct _foo_bar_t3
{
    unpackaged_t x;
};

/**
 * Create a deep copy of a foo_bar_t3.
 * When no longer needed, destroy it with foo_bar_t3_destroy()
 */
foo_bar_t3* foo_bar_t3_copy(const foo_bar_t3* to_copy);

/**
 * Destroy an instance of foo_bar_t3 created by foo_bar_t3_copy()
 */
void foo_bar_t3_destroy(foo_bar_t3* to_destroy);

/**
 * Identifies a single subscription.  This is an opaque data type.
 */
typedef struct _foo_bar_t3_subscription_t foo_bar_t3_subscription_t;

/**
 * Prototype for a callback function invoked when a message of type
 * foo_bar_t3 is received.
 */
typedef void(*foo_bar_t3_handler_t)(const zcm_recv_buf_t* rbuf,
             const char* channel, const foo_bar_t3* msg, void* userdata);

/**
 * Publish a message of type foo_bar_t3 using ZCM.
 *
 * @param zcm The ZCM instance to publish with.
 * @param channel The channel to publish on.
 * @param msg The message to publish.
 * @return 0 on success, <0 on error.  Success means ZCM has transferred
 * responsibility of the message data to the OS.
 */
int foo_bar_t3_publish(zcm_t* zcm, const char* channel, const foo_bar_t3* msg);

/**
 * Subscribe to messages of type foo_bar_t3 using ZCM.
 *
 * @param zcm The ZCM instance to subscribe with.
 * @param channel The channel to subscribe to.
 * @param handler The callback function invoked by ZCM when a message is received.
 *                This function is invoked by ZCM during calls to zcm_handle() and
 *                zcm_handle_timeout().
 * @param userdata An opaque pointer passed to @p handler when it is invoked.
 * @return pointer to subscription type, NULL if failure. Must clean up
 *         dynamic memory by passing the pointer to foo_bar_t3_unsubscribe.
 */
foo_bar_t3_subscription_t* foo_bar_t3_subscribe(zcm_t* zcm, const char* channel, foo_bar_t3_handler_t handler, void* userdata);

/**
 * Removes and destroys a subscription created by foo_bar_t3_subscribe()
 */
int foo_bar_t3_unsubscribe(zcm_t* zcm, foo_bar_t3_subscription_t* hid);
/**
 * Encode a message of type foo_bar_t3 into binary form.
 *
 * @param buf The output buffer.
 * @param offset Encoding starts at this byte offset into @p buf.
 * @param maxlen Maximum number of bytes to write.  This should generally
 *               be equal to foo_bar_t3_encoded_size().
 * @param msg The message to encode.
 * @return The number of bytes encoded, or <0 if an error occured.
 */
int foo_bar_t3_encode(void* buf, uint32_t offset, uint32_t maxlen, const foo_bar_t3* p);

/**
 * Decode a message of type foo_bar_t3 from binary form.
 * When decoding messages containing strings or variable-length arrays, this
 * function may allocate memory.  When finished with the decoded message,
 * release allocated resources with foo_bar_t3_decode_cleanup().
 *
 * @param buf The buffer containing the encoded message
 * @param offset The byte offset into @p buf where the encoded message starts.
 * @param maxlen The maximum number of bytes to read while decoding.
 * @param msg Output parameter where the decoded message is stored
 * @return The number of bytes decoded, or <0 if an error occured.
 */
int foo_bar_t3_decode(const void* buf, uint32_t offset, uint32_t maxlen, foo_bar_t3* msg);

/**
 * Release resources allocated by foo_bar_t3_decode()
 * @return 0
 */
int foo_bar_t3_decode_cleanup(foo_bar_t3* p);

/**
 * Check how many bytes are required to encode a message of type foo_bar_t3
 */
uint32_t foo_bar_t3_encoded_size(const foo_bar_t3* p);
uint32_t foo_bar_t3_struct_size(void);
uint32_t foo_bar_t3_num_fields(void);
int      foo_bar_t3_get_field(const foo_bar_t3* p, uint32_t i, zcm_field_t* f);
const zcm_type_info_t* foo_bar_t3_get_type_info(void);

// ZCM support functions. Users should not call these
int64_t  __foo_bar_t3_get_hash(void);
uint64_t __foo_bar_t3_hash_recursive(const __zcm_hash_ptr* p);
int      __foo_bar_t3_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const foo_bar_t3* p, uint32_t elements);
int      __foo_bar_t3_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, foo_bar_t3* p, uint32_t elements);
int      __foo_bar_t3_decode_array_cleanup(foo_bar_t3* p, uint32_t elements);
uint32_t __foo_bar_t3_encoded_array_size(const foo_bar_t3* p, uint32_t elements);
uint32_t __foo_bar_t3_clone_array(const foo_bar_t3* p, foo_bar_t3* q, uint32_t elements);

#ifdef __cplusplus
}
#endif

#endif
