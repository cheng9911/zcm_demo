// THIS IS AN AUTOMATICALLY GENERATED FILE.
// DO NOT MODIFY BY HAND!!
//
// Generated by zcm-gen

#include <string.h>
#ifndef ZCM_EMBEDDED
#include <stdio.h>
#endif
#include "types/example/zcmtypes/example_zcmtypes_example_t.h"

static int __example_zcmtypes_example_t_hash_computed = 0;
static uint64_t __example_zcmtypes_example_t_hash;

uint64_t __example_zcmtypes_example_t_hash_recursive(const __zcm_hash_ptr* p)
{
    const __zcm_hash_ptr* fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __example_zcmtypes_example_t_get_hash)
            return 0;

    __zcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__example_zcmtypes_example_t_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x1939f4d104c8d2f8LL
         + __int64_t_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __string_hash_recursive(&cp)
         + __boolean_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __example_zcmtypes_example_t_hash_recursive(&cp)
         + __string_hash_recursive(&cp)
         + __int8_t_hash_recursive(&cp)
         + __int8_t_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __example_zcmtypes_example_t_get_hash(void)
{
    if (!__example_zcmtypes_example_t_hash_computed) {
        __example_zcmtypes_example_t_hash = (int64_t)__example_zcmtypes_example_t_hash_recursive(NULL);
        __example_zcmtypes_example_t_hash_computed = 1;
    }

    return __example_zcmtypes_example_t_hash;
}

int __example_zcmtypes_example_t_encode_array(void* buf, uint32_t offset, uint32_t maxlen, const example_zcmtypes_example_t* p, uint32_t elements)
{
    uint32_t pos_byte = 0, element;
    uint32_t pos_bit;
    int thislen;

    for (element = 0; element < elements; ++element) {

        /* timestamp */
        thislen = __int64_t_encode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].timestamp), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* position */
        thislen = __double_encode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].position, 3);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* orientation */
        thislen = __double_encode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].orientation, 4);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* num_ranges */
        thislen = __int32_t_encode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].num_ranges), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* ranges */
        thislen = __int16_t_encode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].ranges, p[element].num_ranges);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* name */
        thislen = __string_encode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].name), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* enabled */
        thislen = __boolean_encode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].enabled), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* nExamples1 */
        thislen = __int32_t_encode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].nExamples1), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* nExamples2 */
        thislen = __int32_t_encode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].nExamples2), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* subExamples */
        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            thislen = __example_zcmtypes_example_t_encode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].subExamples[a], p[element].nExamples2);
            if (thislen < 0) return thislen; else pos_byte += thislen;
        }
        }

        /* subStrings */
        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            thislen = __string_encode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].subStrings[a], p[element].nExamples2);
            if (thislen < 0) return thislen; else pos_byte += thislen;
        }
        }

        // Start of bitfield 0

        pos_bit = 0;

        /* bitfield1 */
        thislen = __int8_t_encode_array_bits(buf, offset + pos_byte, pos_bit, maxlen - pos_byte, &(p[element].bitfield1), 1, 2);
        if (thislen < 0) return thislen;
        __bitfield_advance_offset(&pos_byte, &pos_bit, thislen);

        /* bitfield2 */
        thislen = __int8_t_encode_array_bits(buf, offset + pos_byte, pos_bit, maxlen - pos_byte, p[element].bitfield2, 4, 3);
        if (thislen < 0) return thislen;
        __bitfield_advance_offset(&pos_byte, &pos_bit, thislen);

        if (pos_bit != 0) ++pos_byte;

        // End of bitfield 0
    }
    return pos_byte;
}

int example_zcmtypes_example_t_encode(void* buf, uint32_t offset, uint32_t maxlen, const example_zcmtypes_example_t* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __example_zcmtypes_example_t_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __example_zcmtypes_example_t_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

uint32_t __example_zcmtypes_example_t_encoded_array_size(const example_zcmtypes_example_t* p, uint32_t elements)
{
    uint32_t size = 0, element;
    uint32_t numbits;
    for (element = 0; element < elements; ++element) {

        size += __int64_t_encoded_array_size(&(p[element].timestamp), 1); // timestamp

        size += __double_encoded_array_size(p[element].position, 3); // position

        size += __double_encoded_array_size(p[element].orientation, 4); // orientation

        size += __int32_t_encoded_array_size(&(p[element].num_ranges), 1); // num_ranges

        size += __int16_t_encoded_array_size(p[element].ranges, p[element].num_ranges); // ranges

        size += __string_encoded_array_size(&(p[element].name), 1); // name

        size += __boolean_encoded_array_size(&(p[element].enabled), 1); // enabled

        size += __int32_t_encoded_array_size(&(p[element].nExamples1), 1); // nExamples1

        size += __int32_t_encoded_array_size(&(p[element].nExamples2), 1); // nExamples2

        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            size += __example_zcmtypes_example_t_encoded_array_size(p[element].subExamples[a], p[element].nExamples2); // subExamples
        }
        }

        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            size += __string_encoded_array_size(p[element].subStrings[a], p[element].nExamples2); // subStrings
        }
        }

        numbits = 0;
        numbits += 2; // bitfield1
        numbits += 3; // bitfield2
        size += __bitfield_encoded_size(numbits);
    }
    return size;
}

uint32_t example_zcmtypes_example_t_encoded_size(const example_zcmtypes_example_t* p)
{
    return 8 + __example_zcmtypes_example_t_encoded_array_size(p, 1);
}

uint32_t example_zcmtypes_example_t_struct_size(void)
{
    return sizeof(example_zcmtypes_example_t);
}

uint32_t example_zcmtypes_example_t_num_fields(void)
{
    return 13;
}

int example_zcmtypes_example_t_get_field(const example_zcmtypes_example_t* p, uint32_t i, zcm_field_t* f)
{
    if (i >= example_zcmtypes_example_t_num_fields())
        return 1;
    
    switch (i) {
    
        case 0: {
            f->name = "timestamp";
            f->type = ZCM_FIELD_INT64_T;
            f->typestr = "int64_t";
            f->num_dim = 0;
            f->data = (void*) &p->timestamp;
            return 0;
        }
        
        case 1: {
            f->name = "position";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 1;
            f->dim_size[0] = 3;
            f->dim_is_variable[0] = 0;
            f->data = (void*) &p->position;
            return 0;
        }
        
        case 2: {
            f->name = "orientation";
            f->type = ZCM_FIELD_DOUBLE;
            f->typestr = "double";
            f->num_dim = 1;
            f->dim_size[0] = 4;
            f->dim_is_variable[0] = 0;
            f->data = (void*) &p->orientation;
            return 0;
        }
        
        case 3: {
            f->name = "num_ranges";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->num_ranges;
            return 0;
        }
        
        case 4: {
            f->name = "ranges";
            f->type = ZCM_FIELD_INT16_T;
            f->typestr = "int16_t";
            f->num_dim = 1;
            f->dim_size[0] = p->num_ranges;
            f->dim_is_variable[0] = 1;
            f->data = (void*) &p->ranges;
            return 0;
        }
        
        case 5: {
            f->name = "name";
            f->type = ZCM_FIELD_STRING;
            f->typestr = "string";
            f->num_dim = 0;
            f->data = (void*) &p->name;
            return 0;
        }
        
        case 6: {
            f->name = "enabled";
            f->type = ZCM_FIELD_BOOLEAN;
            f->typestr = "boolean";
            f->num_dim = 0;
            f->data = (void*) &p->enabled;
            return 0;
        }
        
        case 7: {
            f->name = "nExamples1";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->nExamples1;
            return 0;
        }
        
        case 8: {
            f->name = "nExamples2";
            f->type = ZCM_FIELD_INT32_T;
            f->typestr = "int32_t";
            f->num_dim = 0;
            f->data = (void*) &p->nExamples2;
            return 0;
        }
        
        case 9: {
            /* example_t */
            f->name = "subExamples";
            f->type = ZCM_FIELD_USER_TYPE;
            f->typestr = "example.zcmtypes.example_t";
            f->num_dim = 2;
            f->dim_size[0] = p->nExamples1;
            f->dim_size[1] = p->nExamples2;
            f->dim_is_variable[0] = 1;
            f->dim_is_variable[1] = 1;
            f->data = (void*) &p->subExamples;
            return 0;
        }
        
        case 10: {
            f->name = "subStrings";
            f->type = ZCM_FIELD_STRING;
            f->typestr = "string";
            f->num_dim = 2;
            f->dim_size[0] = p->nExamples1;
            f->dim_size[1] = p->nExamples2;
            f->dim_is_variable[0] = 1;
            f->dim_is_variable[1] = 1;
            f->data = (void*) &p->subStrings;
            return 0;
        }
        
        case 11: {
            f->name = "bitfield1";
            f->type = ZCM_FIELD_INT8_T;
            f->typestr = "int8_t:2";
            f->num_dim = 0;
            f->data = (void*) &p->bitfield1;
            return 0;
        }
        
        case 12: {
            f->name = "bitfield2";
            f->type = ZCM_FIELD_INT8_T;
            f->typestr = "int8_t:3";
            f->num_dim = 1;
            f->dim_size[0] = 4;
            f->dim_is_variable[0] = 0;
            f->data = (void*) &p->bitfield2;
            return 0;
        }
        
        default:
            return 1;
    }
}

const zcm_type_info_t* example_zcmtypes_example_t_get_type_info(void)
{
    static int init = 0;
    static zcm_type_info_t typeinfo;
    if (!init) {
        typeinfo.encode         = (zcm_encode_t) example_zcmtypes_example_t_encode;
        typeinfo.decode         = (zcm_decode_t) example_zcmtypes_example_t_decode;
        typeinfo.decode_cleanup = (zcm_decode_cleanup_t) example_zcmtypes_example_t_decode_cleanup;
        typeinfo.encoded_size   = (zcm_encoded_size_t) example_zcmtypes_example_t_encoded_size;
        typeinfo.struct_size    = (zcm_struct_size_t)  example_zcmtypes_example_t_struct_size;
        typeinfo.num_fields     = (zcm_num_fields_t) example_zcmtypes_example_t_num_fields;
        typeinfo.get_field      = (zcm_get_field_t) example_zcmtypes_example_t_get_field;
        typeinfo.get_hash       = (zcm_get_hash_t) __example_zcmtypes_example_t_get_hash;
    }
    
    return &typeinfo;
}

int __example_zcmtypes_example_t_decode_array(const void* buf, uint32_t offset, uint32_t maxlen, example_zcmtypes_example_t* p, uint32_t elements)
{
    uint32_t pos_byte = 0, element;
    uint32_t pos_bit;
    int thislen;

    for (element = 0; element < elements; ++element) {

        /* timestamp */
        thislen = __int64_t_decode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].timestamp), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* position */
        thislen = __double_decode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].position, 3);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* orientation */
        thislen = __double_decode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].orientation, 4);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* num_ranges */
        thislen = __int32_t_decode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].num_ranges), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* ranges */
        p[element].ranges = (int16_t*) zcm_malloc(sizeof(int16_t) * p[element].num_ranges);
        thislen = __int16_t_decode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].ranges, p[element].num_ranges);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* name */
        thislen = __string_decode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].name), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* enabled */
        thislen = __boolean_decode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].enabled), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* nExamples1 */
        thislen = __int32_t_decode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].nExamples1), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* nExamples2 */
        thislen = __int32_t_decode_array(buf, offset + pos_byte, maxlen - pos_byte, &(p[element].nExamples2), 1);
        if (thislen < 0) return thislen; else pos_byte += thislen;

        /* subExamples */
        p[element].subExamples = (example_zcmtypes_example_t**) zcm_malloc(sizeof(example_zcmtypes_example_t*) * p[element].nExamples1);
        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            p[element].subExamples[a] = (example_zcmtypes_example_t*) zcm_malloc(sizeof(example_zcmtypes_example_t) * p[element].nExamples2);
            thislen = __example_zcmtypes_example_t_decode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].subExamples[a], p[element].nExamples2);
            if (thislen < 0) return thislen; else pos_byte += thislen;
        }
        }

        /* subStrings */
        p[element].subStrings = (char***) zcm_malloc(sizeof(char**) * p[element].nExamples1);
        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            p[element].subStrings[a] = (char**) zcm_malloc(sizeof(char*) * p[element].nExamples2);
            thislen = __string_decode_array(buf, offset + pos_byte, maxlen - pos_byte, p[element].subStrings[a], p[element].nExamples2);
            if (thislen < 0) return thislen; else pos_byte += thislen;
        }
        }

        // Start of bitfield 0

        pos_bit = 0;

        /* bitfield1 */
        thislen = __int8_t_decode_array_bits(buf, offset + pos_byte, pos_bit, maxlen - pos_byte, &(p[element].bitfield1), 1, 2);
        if (thislen < 0) return thislen;
        __bitfield_advance_offset(&pos_byte, &pos_bit, thislen);

        /* bitfield2 */
        thislen = __int8_t_decode_array_bits(buf, offset + pos_byte, pos_bit, maxlen - pos_byte, p[element].bitfield2, 4, 3);
        if (thislen < 0) return thislen;
        __bitfield_advance_offset(&pos_byte, &pos_bit, thislen);

        if (pos_bit != 0) ++pos_byte;

        // End of bitfield 0
    }
    return pos_byte;
}

int __example_zcmtypes_example_t_decode_array_cleanup(example_zcmtypes_example_t* p, uint32_t elements)
{
    uint32_t element;
    for (element = 0; element < elements; ++element) {

        __int64_t_decode_array_cleanup(&(p[element].timestamp), 1);

        __double_decode_array_cleanup(p[element].position, 3);

        __double_decode_array_cleanup(p[element].orientation, 4);

        __int32_t_decode_array_cleanup(&(p[element].num_ranges), 1);

        __int16_t_decode_array_cleanup(p[element].ranges, p[element].num_ranges);
        if (p[element].ranges) free(p[element].ranges);

        __string_decode_array_cleanup(&(p[element].name), 1);

        __boolean_decode_array_cleanup(&(p[element].enabled), 1);

        __int32_t_decode_array_cleanup(&(p[element].nExamples1), 1);

        __int32_t_decode_array_cleanup(&(p[element].nExamples2), 1);

        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            __example_zcmtypes_example_t_decode_array_cleanup(p[element].subExamples[a], p[element].nExamples2);
            if (p[element].subExamples[a]) free(p[element].subExamples[a]);
        }
        }
        if (p[element].subExamples) free(p[element].subExamples);

        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            __string_decode_array_cleanup(p[element].subStrings[a], p[element].nExamples2);
            if (p[element].subStrings[a]) free(p[element].subStrings[a]);
        }
        }
        if (p[element].subStrings) free(p[element].subStrings);

        __int8_t_decode_array_cleanup(&(p[element].bitfield1), 1);

        __int8_t_decode_array_cleanup(p[element].bitfield2, 4);

    }
    return 0;
}

int example_zcmtypes_example_t_decode(const void* buf, uint32_t offset, uint32_t maxlen, example_zcmtypes_example_t* p)
{
    uint32_t pos = 0;
    int thislen;
    int64_t hash = __example_zcmtypes_example_t_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __example_zcmtypes_example_t_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int example_zcmtypes_example_t_decode_cleanup(example_zcmtypes_example_t* p)
{
    return __example_zcmtypes_example_t_decode_array_cleanup(p, 1);
}

uint32_t __example_zcmtypes_example_t_clone_array(const example_zcmtypes_example_t* p, example_zcmtypes_example_t* q, uint32_t elements)
{
    uint32_t n = 0, element;
    for (element = 0; element < elements; ++element) {

        n += __int64_t_clone_array(&(p[element].timestamp), &(q[element].timestamp), 1);

        n += __double_clone_array(p[element].position, q[element].position, 3);

        n += __double_clone_array(p[element].orientation, q[element].orientation, 4);

        n += __int32_t_clone_array(&(p[element].num_ranges), &(q[element].num_ranges), 1);

        q[element].ranges = (int16_t*) zcm_malloc(sizeof(int16_t) * q[element].num_ranges);
        n += __int16_t_clone_array(p[element].ranges, q[element].ranges, p[element].num_ranges);

        n += __string_clone_array(&(p[element].name), &(q[element].name), 1);

        n += __boolean_clone_array(&(p[element].enabled), &(q[element].enabled), 1);

        n += __int32_t_clone_array(&(p[element].nExamples1), &(q[element].nExamples1), 1);

        n += __int32_t_clone_array(&(p[element].nExamples2), &(q[element].nExamples2), 1);

        q[element].subExamples = (example_zcmtypes_example_t**) zcm_malloc(sizeof(example_zcmtypes_example_t*) * q[element].nExamples1);
        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            q[element].subExamples[a] = (example_zcmtypes_example_t*) zcm_malloc(sizeof(example_zcmtypes_example_t) * q[element].nExamples2);
            n += __example_zcmtypes_example_t_clone_array(p[element].subExamples[a], q[element].subExamples[a], p[element].nExamples2);
        }
        }

        q[element].subStrings = (char***) zcm_malloc(sizeof(char**) * q[element].nExamples1);
        { int a;
        for (a = 0; a < p[element].nExamples1; ++a) {
            q[element].subStrings[a] = (char**) zcm_malloc(sizeof(char*) * q[element].nExamples2);
            n += __string_clone_array(p[element].subStrings[a], q[element].subStrings[a], p[element].nExamples2);
        }
        }

        n += __int8_t_clone_array(&(p[element].bitfield1), &(q[element].bitfield1), 1);

        n += __int8_t_clone_array(p[element].bitfield2, q[element].bitfield2, 4);

    }
    return n;
}

example_zcmtypes_example_t* example_zcmtypes_example_t_copy(const example_zcmtypes_example_t* p)
{
    example_zcmtypes_example_t* q = (example_zcmtypes_example_t*) malloc(sizeof(example_zcmtypes_example_t));
    __example_zcmtypes_example_t_clone_array(p, q, 1);
    return q;
}

void example_zcmtypes_example_t_destroy(example_zcmtypes_example_t* p)
{
    __example_zcmtypes_example_t_decode_array_cleanup(p, 1);
    free(p);
}

int example_zcmtypes_example_t_publish(zcm_t* zcm, const char* channel, const example_zcmtypes_example_t* p)
{
      uint32_t max_data_size = example_zcmtypes_example_t_encoded_size (p);
      uint8_t* buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = example_zcmtypes_example_t_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = zcm_publish (zcm, channel, buf, (uint32_t)data_size);
      free (buf);
      return status;
}

struct _example_zcmtypes_example_t_subscription_t {
    example_zcmtypes_example_t_handler_t user_handler;
    void* userdata;
    zcm_sub_t* z_sub;
};
static
void example_zcmtypes_example_t_handler_stub (const zcm_recv_buf_t* rbuf,
                            const char* channel, void* userdata)
{
    int status;
    example_zcmtypes_example_t p;
    memset(&p, 0, sizeof(example_zcmtypes_example_t));
    status = example_zcmtypes_example_t_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        #ifndef ZCM_EMBEDDED
        fprintf (stderr, "error %d decoding example_zcmtypes_example_t!!!\n", status);
        #endif
        return;
    }

    example_zcmtypes_example_t_subscription_t* h = (example_zcmtypes_example_t_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    example_zcmtypes_example_t_decode_cleanup (&p);
}

example_zcmtypes_example_t_subscription_t* example_zcmtypes_example_t_subscribe (zcm_t* zcm,
                    const char* channel,
                    example_zcmtypes_example_t_handler_t f, void* userdata)
{
    example_zcmtypes_example_t_subscription_t* n = (example_zcmtypes_example_t_subscription_t*)
                       malloc(sizeof(example_zcmtypes_example_t_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->z_sub = zcm_subscribe (zcm, channel,
                              example_zcmtypes_example_t_handler_stub, n);
    if (n->z_sub == NULL) {
        #ifndef ZCM_EMBEDDED
        fprintf (stderr,"couldn't reg example_zcmtypes_example_t ZCM handler!\n");
        #endif
        free (n);
        return NULL;
    }
    return n;
}

int example_zcmtypes_example_t_unsubscribe(zcm_t* zcm, example_zcmtypes_example_t_subscription_t* hid)
{
    int status = zcm_unsubscribe (zcm, hid->z_sub);
    if (0 != status) {
        #ifndef ZCM_EMBEDDED
        fprintf(stderr,
           "couldn't unsubscribe example_zcmtypes_example_t_handler %p!\n", hid);
        #endif
        return -1;
    }
    free (hid);
    return 0;
}

