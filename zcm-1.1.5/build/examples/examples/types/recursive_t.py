"""ZCM type definitions
This file automatically generated by zcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

from example_t import example_t

class recursive_t(object):
    __slots__ = ["e"]

    IS_LITTLE_ENDIAN = False;
    def __init__(self):
        self.e = example_t()

    def encode(self):
        buf = BytesIO()
        buf.write(recursive_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert self.e._get_packed_fingerprint() == example_t._get_packed_fingerprint()
        self.e._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != recursive_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return recursive_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = recursive_t()
        self.e = example_t._decode_one(buf)
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if recursive_t in parents: return 0
        newparents = parents + [recursive_t]
        tmphash = (0x24dfa672b5d5c433+ example_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + ((tmphash>>63)&0x1)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if recursive_t._packed_fingerprint is None:
            recursive_t._packed_fingerprint = struct.pack(">Q", recursive_t._get_hash_recursive([]))
        return recursive_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

